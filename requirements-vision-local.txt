# requirements-vision-local.txt · M3 v1.6.0
# Local Vision 模式依赖（PaddleOCR PP-StructureV3）
#
# ⚠️ PaddlePaddle 引擎不在本文件中，需单独通过 scripts/install_paddle.sh 安装
#    原因：CPU/GPU 版本二选一，且 PaddlePaddle 需要从官方镜像源装，
#          与 PyPI 生态解耦
#
# ⚠️ Phase 1 不装本文件；Phase 2 才启用

paddleocr[doc-parser]>=3.0.0,<4.0.0
opencv-python-headless>=4.10.0,<5.0.0
pillow>=10.0.0,<12.0.0
numpy>=1.26.0,<3.0.0
