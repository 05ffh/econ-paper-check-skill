# requirements-kb.txt · M3 v1.6.0 · v0.3.1 轻量化
# 可选 · KB-B 范例向量检索
#
# 说明：
#   - KB-A 规范层是纯 YAML，不依赖任何向量库（已在 requirements-core.txt 覆盖）
#   - Core 装完就能跑 DOCX/PDF 主流程 + KB-A 规范查询
#   - 装完本文件后 KB-B（范例参照卡）能力才启用
#
# 未启用时的降级（v0.3.1 P0-1）：
#   - kb_query.py --prefer B 检测 chromadb 未装 → 返回空 list + notice
#   - build_reference_card.py 报告不出现"参照卡"段落
#   - doctor.py [2] 分组：KB-A ✅ 18 条 · KB-B ⚪ 未启用

chromadb>=0.5.23,<0.6.0
sentence-transformers>=3.3.1,<4.0.0
# Embedding 模型: BAAI/bge-small-zh-v1.5（CPU · 512 维 · M1 已在跑）
