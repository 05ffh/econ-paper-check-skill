# requirements-core.txt · M3 v1.6.0
# 经管论文智检 Skill Core 模式依赖
# Phase 1 Build Gate 边界：只装本文件即可跑 Core 模式（无视觉、无向量库）
#
# 严禁引入：chromadb / sentence-transformers / paddleocr / paddlepaddle / volcengine-*

# DOCX 解析
python-docx>=1.1.0,<2.0.0

# PDF 文本层
pypdf>=4.0.0,<6.0.0
pdfplumber>=0.11.0,<0.12.0

# 配置与模板
pyyaml>=6.0.0,<7.0.0
jinja2>=3.1.0,<4.0.0

# XML/HTML 处理（python-docx 底层依赖，显式列出方便锁版本）
lxml>=5.0.0,<6.0.0
